# Logger V1 implementation specification

This document was renamed when the product became **Kept**. Read the canonical specification at [Kept V1 implementation specification](./kept-v1-implementation-spec.md).

The old path remains as a compatibility stub so existing GitHub links and bookmarks continue to resolve while `Logger` is retained only as the historical product name.
