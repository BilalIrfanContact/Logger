export default function JournalShellPage() {
  return (
    <section className="app-empty-state" aria-labelledby="app-title">
      <p className="eyebrow">Your private work journal</p>
      <h1 id="app-title">A quiet place for what happened today.</h1>
      <p>This authenticated shell is ready. Journal capture and review arrive in the next Kept release.</p>
    </section>
  );
}
